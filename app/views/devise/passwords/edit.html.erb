<div class="max-w-md mx-auto bg-white shadow-lg rounded-xl border border-gray-200 p-8 mt-12">
  <div class="text-center mb-8">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-indigo-600 mb-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />
    </svg>
    <h2 class="text-2xl font-bold text-indigo-900">Zmień swoje hasło</h2>
    <p class="text-gray-600 mt-1">Wprowadź nowe hasło, aby kontynuować</p>
  </div>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: "space-y-6" }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <%= f.hidden_field :reset_password_token %>

    <div class="space-y-1">
      <%= f.label :password, "Nowe hasło", class: "block text-sm font-medium text-gray-700" %>
      <% if @minimum_password_length %>
        <span class="text-xs text-gray-500">minimum <%= @minimum_password_length %> znaków</span>
      <% end %>
      <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500" %>
    </div>

    <div class="space-y-1">
      <%= f.label :password_confirmation, "Potwierdzenie nowego hasła", class: "block text-sm font-medium text-gray-700" %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500" %>
    </div>

    <div>
      <%= f.submit "Zmień moje hasło", class: "w-full py-3 px-4 bg-gradient-to-r from-indigo-700 to-blue-900 hover:from-indigo-800 hover:to-blue-950 text-white font-bold rounded-lg shadow-lg focus:outline-none transition-all hover:shadow-xl" %>
    </div>
  <% end %>

  <div class="mt-6 text-center">
    <%= link_to "Wróć do logowania", new_session_path(resource_name), class: "text-indigo-600 hover:text-indigo-800 text-sm font-medium" %>
  </div>
</div>